require 'spec_helper'

describe Kantox::Marriage do
  it 'has a version number' do
    expect(Kantox::Marriage::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
